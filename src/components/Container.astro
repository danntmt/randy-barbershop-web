---
interface Props {
  as?: string;
  class?: string;
  size?: 'xl';
}

const {
  as: Tag = 'div',
  class: className = '',
  size = 'xl',
} = Astro.props;
---

<Tag class:list={['mx-auto px-5 md:px-8 lg:px-12 w-full max-w-7xl', className]}>
  <slot />
</Tag>
